{
  "name": "fit-trackerpro",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,green \"cd backend && python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000\" \"cd frontend && npm run dev\"",
    "dev:infra": "docker compose up -d postgres redis",
    "frontend:dev": "cd frontend && npm run dev",
    "backend:dev": "cd backend && python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
