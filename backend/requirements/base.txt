# FastAPI and Web Framework
# prometheus-fastapi-instrumentator pinned >=8: the 7.x middleware crashes on
# starlette>=0.50 routing ('_IncludedRouter' has no attribute 'path' → 500 on
# every /api/v1/* request); 8.x is built for the new routing and requires
# starlette>=1.0, which fastapi 0.141.x accepts.
prometheus-fastapi-instrumentator==8.1.0
fastapi==0.141.1
uvicorn[standard]==0.53.0
python-multipart==0.0.32
gunicorn==26.2.0

# Database
sqlalchemy==2.0.54
alembic==1.20.0
asyncpg==0.31.0
psycopg[binary]==3.3.5

# Pydantic and Validation
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0

# Authentication and Security
# PyJWT instead of python-jose: python-jose 3.5.0 (the latest) pins `ecdsa`, which
# is flagged by pip-audit (PYSEC-2026-1325) and cannot be updated independently.
# Only HS256 is used (see app/core/security/tokens.py), so PyJWT + cryptography
# covers every code path without the vulnerable transitive dependency.
pyjwt==2.14.0
passlib[bcrypt]==1.7.4
python-telegram-bot==22.8

# HTTP Client
httpx==0.28.1
aiohttp==3.14.3

# Utilities
python-dotenv==1.2.3
orjson==3.12.0
redis==7.4.0
slowapi==0.1.10

# Logging and Monitoring
structlog==26.1.0
sentry-sdk==2.69.2
