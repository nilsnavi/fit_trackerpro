/**
 * Версии правовых документов — без самих текстов.
 *
 * Этот модуль импортируется в онбординг (то есть попадает во входной чанк),
 * поэтому здесь только строки версий. Полные тексты живут в `content.ts`,
 * который грузится вместе со страницей `/legal/:doc`.
 *
 * Версии обязаны совпадать с `backend/app/core/legal.py`.
 */

export const PRIVACY_POLICY_VERSION = '2026-09-23'
export const HEALTH_DATA_CONSENT_VERSION = '2026-09-23'
